Se você estiver testando localmente, certifique-se de que o servidor Apache
está configurado para permitir o uso de arquivos .htaccess. Isso é controlado
 pela diretiva AllowOverride no arquivo de configuração do Apache (httpd.conf), que 
precisa ser definido como All ou FileInfo para permitir o uso do
 mod_rewrite em um arquivo .htaccess.

 PHP Version 8.0.25
 Versão do servidor: 8.0.26 - MySQL Community Server - GPL

 Alterar os dados do banco de dados no arquivo config.php

Certifiquese de ter o composer instalado

composer update
